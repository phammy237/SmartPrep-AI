# SmartPrep Ingredient Model workspace - install with:
#   pip install -r requirements.txt
# CPU-only PyTorch wheels are on a separate index; if this plain install pulls
# a CUDA build you don't want, install torch/torchvision first from
# https://download.pytorch.org/whl/cpu, then re-run this file.
torch>=2.2
torchvision>=0.17
numpy>=1.26
Pillow>=10.0
PyYAML>=6.0
scikit-learn>=1.3
matplotlib>=3.8
tqdm>=4.66
pytest>=8.0
