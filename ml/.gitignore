# Datasets, model weights, and generated run outputs are never committed -
# see ml/README.md ("Reproducibility") for how to regenerate them instead.

# Raw/processed images and the generated split manifest.
data/raw/*
data/processed/*
data/splits/*
!data/raw/.gitkeep
!data/processed/.gitkeep
!data/splits/.gitkeep

# Images downloaded by ml/scripts/acquire/* scripts. Never committed - only
# the small provenance CSV (data/provenance/*.csv, relative paths, no image
# bytes) and the acquisition script itself are: anyone can reproduce the
# exact same files by re-running the script against the recorded source.
data/raw_acquired/*
!data/raw_acquired/.gitkeep

# First-party photos staged for import (scripts/import_first_party.py).
# Never committed - real photos of real ingredients live here only until
# imported into data/raw/, same as every other raw-image location above.
data/first_party_inbox/*
!data/first_party_inbox/.gitkeep

# Every training run's artifacts (checkpoints, metrics, plots). Small,
# individually-interesting metrics.json/config.json files can still be copied
# out and committed elsewhere deliberately if ever wanted - this only ignores
# the bulk run-output tree by default.
outputs/*
!outputs/.gitkeep

# Model weight / experiment-cache file types, wherever they end up.
*.pt
*.pth
*.onnx
*.tflite
*.mlmodel
*.ckpt
*.h5

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/
